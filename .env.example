# ══════════════════════════════════════════════════════════════════════════════
# NEXUS — Environment Configuration Template
# Copy to .env and fill in your values. Never commit .env.
# ══════════════════════════════════════════════════════════════════════════════

# ── Local LLM (Ollama) — Recommended for PRIVATE tier ────────────────────────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_DEFAULT_MODEL=qwen2.5:7b

# ── Cloud LLM Providers (optional — used for INTERNAL/PUBLIC tiers) ──────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...

# ── Vector Store (ChromaDB) ───────────────────────────────────────────────────
CHROMA_HOST=localhost
CHROMA_PORT=8000
CHROMA_PERSIST_PATH=./data/vector_store

# ── Graph Database (Neo4j) — optional, falls back to NetworkX ─────────────────
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password-here

# ── Cache (Redis) — optional, used for session short-term memory ──────────────
REDIS_URL=redis://localhost:6379

# ── MCP Server — PostgreSQL ───────────────────────────────────────────────────
POSTGRES_URL=postgresql://user:password@localhost:5432/nexus

# ── MCP Server — GitHub ───────────────────────────────────────────────────────
GITHUB_TOKEN=ghp_...

# ── MCP Server — Slack ────────────────────────────────────────────────────────
SLACK_BOT_TOKEN=xoxb-...
SLACK_DEFAULT_CHANNEL=#ai-ops

# ── MCP Server — MLflow ───────────────────────────────────────────────────────
MLFLOW_TRACKING_URI=http://localhost:5000
# MLFLOW_USERNAME=admin          # Only if MLflow has auth enabled
# MLFLOW_PASSWORD=password

# ── Email Agent ───────────────────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASSWORD=app-specific-password
# Or use Resend API:
# RESEND_API_KEY=re_...

# ── A2A Agent (Google Agent-to-Agent Protocol) ────────────────────────────────
NEXUS_PUBLIC_URL=http://localhost:7800    # Used in AgentCard discovery
A2A_API_KEY=your-a2a-key                 # For authenticating incoming A2A requests

# ── Governance ────────────────────────────────────────────────────────────────
NEXUS_DAILY_BUDGET_USD=5.00
NEXUS_MONTHLY_BUDGET_USD=100.00

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO                           # DEBUG | INFO | WARNING | ERROR
NEXUS_AUDIT_DB_PATH=./data/audit.db

# ── Data Lineage ──────────────────────────────────────────────────────────────
NEXUS_LINEAGE_FILE=./data/lineage_graph.json
